{
	"name": "update-example",
	"version": "2.1.0",
	"description": "test",
	"main": "main.js",
	"repository": {
		"type": "git",
		"url": "https://github.com/FranciscoHeronildoCleveriano/update-example.git"
	},
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "electron .",
		"build": "electron-builder build --win --publish never",
		"deploy": "electron-builder build --win --publish always"
	},
	"author": "FranciscoHeronildoCleveriano",
	"license": "ISC",
	"devDependencies": {
		"electron": "^27.0.2",
		"electron-builder": "^24.6.4"
	},
	"dependencies": {
		"electron-updater": "^6.1.4"
	}
}
